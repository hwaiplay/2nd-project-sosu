<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="moimlist">
	
	<!--  -->	
	<select id="m" resultType="hashmap" parameterType="hashmap">
		
		SELECT 
				
		FROM 
				
		WHERE 
						
	</select>
	
	<!--  -->
	<insert id="n" parameterType="hashmap">
	
		INSERT INTO 
				
				VALUES
				(
						)
	</insert>
	
	<!--  -->
	<update id="o" parameterType="hashmap">
	
        UPDATE 
        		
		SET 
				
		WHERE 
				    
	</update>
	
	<!--  -->
	<delete id="p" parameterType="hashmap">
	
    	DELETE FROM  
        		
        WHERE 
       
	</delete>

</mapper>