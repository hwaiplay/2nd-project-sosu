<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="freeboard">
	
	<!--  -->	
	<select id="e" resultType="hashmap" parameterType="hashmap">
		
		SELECT 
				
		FROM 
				
		WHERE 
						
	</select>
	
	<!--  -->
	<insert id="f" parameterType="hashmap">
	
		INSERT INTO 
				
				VALUES
				(
						)
	</insert>
	
	<!--  -->
	<update id="g" parameterType="hashmap">
	
        UPDATE 
        		
		SET 
				
		WHERE 
				    
	</update>
	
	<!--  -->
	<delete id="h" parameterType="hashmap">
	
    	DELETE FROM  
        		
        WHERE 
       
	</delete>

</mapper>