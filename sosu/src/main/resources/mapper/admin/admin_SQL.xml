<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin">
	
	<!--  -->	
	<select id="a" resultType="hashmap" parameterType="hashmap">
		
		SELECT 
				
		FROM 
				
		WHERE 
						
	</select>
	
	<!--  -->
	<insert id="b" parameterType="hashmap">
	
		INSERT INTO 
				
				VALUES
				(
						)
	</insert>
	
	<!--  -->
	<update id="c" parameterType="hashmap">
	
        UPDATE 
        		
		SET 
				
		WHERE 
				    
	</update>
	
	<!--  -->
	<delete id="d" parameterType="hashmap">
	
    	DELETE FROM  
        		
        WHERE 
       
	</delete>

</mapper>